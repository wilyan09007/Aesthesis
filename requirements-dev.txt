# Dev / test dependencies. Lightweight — runs without GPU and without ffmpeg/Gemini.

pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
httpx>=0.27          # used by FastAPI TestClient and as a real client in smoke tests
numpy>=1.26
pydantic>=2.5
fastapi>=0.110
