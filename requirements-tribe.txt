# TRIBE v2 service dependencies. Installed inside the GPU container.
# See DESIGN.md §5.9 for the canonical list.

# Service plumbing
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
arq>=0.26
redis>=5.0

# TRIBE v2 (installed from GitHub on the GPU image, here for reference)
# pip install git+https://github.com/facebookresearch/tribev2

# GPU / science
torch>=2.1
torchaudio>=2.1
torchvision>=0.16
numpy>=1.26
scipy>=1.11
pandas>=2.0

# Neuroimaging
nilearn>=0.10
nibabel>=5.1
nimare>=0.3
nltools>=0.5

# Mesh bake (one-time cortical GLB generation; NOT on the request path).
# Used by tribe_service/scripts/bake_brain_glbs.py to write fsaverage5
# cortical meshes with custom _PARCELID / _SULC vertex attributes that
# the frontend (BrainCortical.tsx) consumes via drei's useGLTF.
# See ASSUMPTIONS_BRAIN.md §2.
pygltflib>=1.16
trimesh>=4.0

# Audio (used by TRIBE's get_events_dataframe pipeline)
whisperx==3.1.1
edge-tts>=6.1

# Video probing (used by the service for sanity-checking incoming MP4 paths)
ffmpeg-python>=0.2

# Utilities
httpx>=0.27
python-multipart>=0.0.9
